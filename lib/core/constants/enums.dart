enum UserRole {
  floorWorker,
  supervisor,
  maintenance,
}

enum TicketStatus {
  open,
  assigned,
  pendingValidation,
  closed,
  rejected,
}

enum HazardCategory {
  electrical,
  chemical,
  structural,
  janitorial,
}

enum UrgencyLevel {
  low,
  medium,
  high,
  critical,
}
