// STUB — replace with Module A implementation. Do not change filename or class name.
import 'package:flutter/material.dart';
import 'package:laporpak_fp/core/models/app_user.dart';

class WorkerHomePage extends StatelessWidget {
  final AppUser user;

  const WorkerHomePage({super.key, required this.user});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Module A — Floor Worker (built by teammate A)'),
    );
  }
}
