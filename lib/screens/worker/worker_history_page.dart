// STUB — replace with Module A history implementation. Do not change filename or class name.
import 'package:flutter/material.dart';
import 'package:laporpak_fp/core/models/app_user.dart';

class WorkerHistoryPage extends StatelessWidget {
  final AppUser user;

  const WorkerHistoryPage({super.key, required this.user});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Worker History — Module A (built by teammate A)'),
    );
  }
}
