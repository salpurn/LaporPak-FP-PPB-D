// STUB — replace with Module B implementation. Do not change filename or class name.
import 'package:flutter/material.dart';
import 'package:laporpak_fp/core/models/app_user.dart';

class SupervisorHomePage extends StatelessWidget {
  final AppUser user;

  const SupervisorHomePage({super.key, required this.user});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Module B — Supervisor (built by teammate B)'),
    );
  }
}
