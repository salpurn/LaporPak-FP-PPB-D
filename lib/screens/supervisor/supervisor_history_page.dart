// STUB — replace with Module B history implementation. Do not change filename or class name.
import 'package:flutter/material.dart';
import 'package:laporpak_fp/core/models/app_user.dart';

class SupervisorHistoryPage extends StatelessWidget {
  final AppUser user;

  const SupervisorHistoryPage({super.key, required this.user});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Supervisor History — Module B (built by teammate B)'),
    );
  }
}
